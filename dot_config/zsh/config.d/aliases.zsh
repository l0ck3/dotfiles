# VSCode

alias code='codium'

# LS 

alias ls='lsd'
alias ll='ls -l'
alias la='ls -la'
alias lt='ls --tree'

# Git

alias gst='git status'

# Kubernetes

alias k='kubectl'
alias wk='watch kubectl'

# SHELL 

alias reload!='exec "$SHELL" -l'
